{
  "name": "weather_app_improved",
  "version": "1.0.0",
  "description": "a simple weather app with a nodejs backend fronted by react",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/app.js",
    "build": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": "v18.10.0"
  }
}
